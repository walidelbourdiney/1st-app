import React from "react";

const Header = () => {
  return (
    <header
      style={{
        backgroundColor: "mediumblue",
        color: "#fff",
        width: "80%",
        margin: "auto",
      }}
    >
      <h1>Groceries List</h1>
    </header>
  );
};

export default Header;
